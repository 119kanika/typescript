// enums are a special class that represents a group of constants.
// they come in as string and numeric

const enum SeatChoice {
    AISLE = "aisle",
    MIDDLE = 3,
    WINDOW,

}

const seat = SeatChoice.MIDDLE 


export {}