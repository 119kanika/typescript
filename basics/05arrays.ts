const superHeroes: string[] = []
const heroPower: number[] = []

const numArray: Array<number> = []

type User = {
    name: string,
    isActive: boolean
}

const allUsers: User[] = []

const models: number[][] = [
    [255, 255, 255],
    [344, 344, 344]
]

superHeroes.push("Kanika")
heroPower.push(2)
numArray.push(33)

allUsers.push({name:"k", isActive:false})